<!-- oh boy, what's happening here? why isn't this in the stylesheet? -->
<style>
  .header {
    background: linear-gradient(45deg,
      rgba(0, 0, 0, 0.45),
      rgba(0, 0, 0, 0.45)
    ), url("<%= session[:dummy_restaurant].photo %>") center;
    background-size: cover;
  }
</style>


<div class="header">
  <h1><%= session[:dummy_restaurant].name %></h1>
</div>

<div class="socials">
  <% socials = {phone: session[:dummy_restaurant].phone, facebook: session[:dummy_restaurant].facebook, twitter: session[:dummy_restaurant].twitter, globe: session[:dummy_restaurant].url}.compact %>
  <!-- I think a better approach would be to work with model objects, or shells of model objects. In the controller, you could have rendered the show view with the initialization of the dummy object read from either the object or the session if you were so concerned wwith db rows. However, I don't think that the extra data should be a huge concern here. -->
  <% socials.each do |k, v| %>
  <a href="
    <% case k %>
    <% when :phone %>
    <%= "tel:#{v.tr('^0-9','')}" %>
    <% when :facebook %>
    <%= "https://facebook.com/#{v}" %>
    <% when :twitter %>
    <%= "https://twitter.com/#{v}" %>
    <% when :globe %>
    <%= v %>
    <% end %>
  ">
    <i class="fa fa-<%= k.to_s %> fa-lg"></i>
  </a>
  <% end %>
</div>

<h4 id="review_list_header">Reviews list</h4>
<p>None here yet — be the first!</p>
<%= link_to 'Create a new review for this restaurant', dummy_review_path %>
