<h1>Palatable</h1>

<% if flash[:message] %>
  <%= flash[:message] %>
<% end %>

<%= form_for @search do |f| %>
  <%= f.text_field :search, placeholder: 'Search', required: true %>
  <%= f.text_field :location, placeholder: 'ZIP or Address', required: true %>
  <%= f.submit 'Search' %>
<% end %>
